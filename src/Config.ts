import { GameConfig } from './features/breakout-game/types'

export const GAME_CONFIG: GameConfig = {
  ballRadius: 10,
  paddleHeight: 10,
  paddleWidth: 100,
  brickRowCount: 5,
  brickColumnCount: 9,
  brickWidth: 75,
  brickHeight: 20,
  brickPadding: 10,
  brickOffsetTop: 60,
  brickOffsetLeft: 30,
  canvasWidth: 800,
  canvasHeight: 600,
}